<div class="container" id="thirds_new">

	<!-- Head -->
	<div class="row">
		<div class="col-xs-12 margin-head final-margin-lawyers">
			<h1>Cadastros > Terceiros > <%= link_to @name_to_show, send("index_#{@path}_path") %> > <a href="<%= edit_third_path(@third) %>"><%=@third.register_number%></a></h1>
		</div>
	</div>

	<div class="row">
		<div class="col-xs-12">
			<%= render 'form' %>
		</div>
	</div>

</div>